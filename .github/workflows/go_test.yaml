name: go-test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: self-hosted
    container:
      image: ghcr.io/openai/codex-universal:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run tests
        run: go test ./...
