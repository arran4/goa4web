Usage:
  {{.Prog}} grant <command> [<args>]

Commands:
  add               create a grant rule
  delete            delete a grant rule
  export            export grant definitions as JSON
  list              list grant rules
  list-available    list all available grant rules

{{template "flag_groups_section" .FlagGroups}}
