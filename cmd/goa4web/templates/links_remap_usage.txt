Usage:
  {{.Prog}} links remap <command> [<args>]

Commands:
  extract    scan content to discover external links and export to CSV
  apply      apply URL remappings from CSV

Examples:
  {{.Prog}} links remap extract > links.csv
  {{.Prog}} links remap apply -file links.csv

{{template "flag_groups_section" .FlagGroups}}
