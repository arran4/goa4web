Usage:
  {{.Prog}} links sign [<flags>] <url>

Flags:
  -duration string  validity duration (default "24h")
  -no-expiry        sign link with no expiration

{{template "flag_groups_section" .FlagGroups}}
