Usage:
  {{.Prog}} test <command> [<args>]

Commands:
  migrations  run migration tests

Examples:
  {{.Prog}} test migrations apply all -database-type mysql -connection-string "user:pass@tcp(localhost)/a4web"
  {{.Prog}} test migrations clean -database-type mysql -connection-string "user:pass@tcp(localhost)/a4web"

{{template "flag_groups_section" .FlagGroups}}
