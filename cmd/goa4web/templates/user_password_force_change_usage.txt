Usage:
  {{.Prog}} user password force-change [--username USERNAME | --user-id USER_ID]

Forces a password change for a user to a randomly generated password.

This command generates a random password, updates the user's password hash, clears any existing password reset requests for the user, and prints the new password to standard output.

Options:
{{template "flag_groups_section" .FlagGroups}}
