package templates

import "github.com/arran4/goa4web/core/common"

@Head(cd, $)

    <h2 class="section-title">{ data.Username }'s links.</h2>
    @GetAllLinkerItemsByCategoryIdWitherPosterUsernameAndCategoryTitleDescendingLinks(cd, $)

@Tail(cd, $)
