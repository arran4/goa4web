package templates

import "github.com/arran4/goa4web/core/common"

templ WritingsCategoryPage(cd *common.CoreData, data any) {

    @Head(cd, $)

        @ListWritingCategories(cd, $)

        @ListAbstracts(cd, $)

    @Tail(cd, $)

}
