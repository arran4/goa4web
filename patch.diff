--- handlers/blogs/matchers.go
+++ handlers/blogs/matchers.go
@@ -118,7 +118,7 @@
			return
		}

-		if !cd.HasGrant("blogs", "entry", "post", 0) {
+		if !(cd.IsAdmin() || cd.HasGrant("blogs", "entry", "post", 0)) {
			handlers.RenderErrorPage(w, r, handlers.ErrForbidden)
			return
		}
