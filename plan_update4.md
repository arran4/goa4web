The user PR comment asks: "is it possible to add this or to use the bus for this rather than a goroutine?" referring to line 133 `go func() {`.
I need to reply to the user. I can reply saying: "Yes, it is possible. However, the background task worker (`BackgroundTasker`) runs completely detached from the HTTP request and does not have access to the `*common.CoreData` or its `cd.HTTPClient()`, `cd.EnsureExternalLink`, etc. Refactoring it to use `BackgroundTasker` would require passing down the HTTP client and configuration, or duplicating the database update logic using just the provided `db.Querier`. Given that `ReloadExternalLinkTask` also currently uses a goroutine for this exact reason, it's consistent with the existing module structure. Should I go ahead and refactor both tasks to use the event bus, or keep the goroutine for simplicity?"

Wait, is it actually hard? Let's check `core/common/coredata.go` to see if `CoreData` has an easy way to run background tasks with `cd`, or if `BackgroundTasker` gives us enough context.
`BackgroundTask(ctx context.Context, q db.Querier)` gives us `q`, which is a `db.Querier` containing all `cd.queries` methods like `EnsureExternalLink`!
What about `cd.HTTPClient()`? The background worker can just create a new `opengraph.NewSafeClient()` or `http.DefaultClient`.
What about `cd.DownloadAndCacheImage`? That method belongs to `*CoreData`. We might need to pass `cd.Config` to the task struct so we can instantiate `upload.ProviderFromConfig` and do what `DownloadAndCacheImage` does, or we can just pass the `*CoreData` object itself? No, `*CoreData` is request-scoped, its DB connection might be closed, though it's typically fine since it just holds config and queries. Wait, the `backgroundtaskworker` runs in a separate goroutine and creates its own `db.Querier` transactions. It's unsafe to use `*CoreData` methods that rely on the request context or transaction.

Let's check if there is an `externallinkworker`! The memory says: `workers/externallinkworker/worker.go`. Let's see what that does.
